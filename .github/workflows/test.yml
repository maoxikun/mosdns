name: Test mosdns

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: "1.26"
          check-latest: true
          cache: false

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -race -v ./...
